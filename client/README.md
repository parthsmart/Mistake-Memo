# MistakeMemo client

React/Vite frontend for MistakeMemo. Run `npm run dev` while the API is running from `../server`; Vite proxies `/api` to `http://localhost:5000`.

Set `VITE_API_URL` when the API is hosted elsewhere. Its value should include the `/api` suffix, for example `https://api.example.com/api`.
